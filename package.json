{
  "name": "autonomous-data",
  "private": true,
  "scripts": {
    "dev": "vuepress dev site",
    "build": "vuepress build site",
    "publish": "scripts/publish.sh"
  },
  "devDependencies": {
    "vuepress": "^0.14.8"
  }
}
